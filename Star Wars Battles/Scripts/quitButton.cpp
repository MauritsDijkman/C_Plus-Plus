//#include "quitButton.hpp"
//
//QuitButton::QuitButton(std::string identifier, sf::Font& font, std::string buttonText,
//	sf::Vector2f size, bool wantHover, sf::Color normalColor, sf::Color hoverColor, sf::RenderWindow& window) :
//	Button(identifier, font, buttonText, size, wantHover, normalColor, hoverColor), window(window) { } // Uses the button template to display the button
//
//// Deletes everything after code has been runned
//QuitButton::~QuitButton() { }
//
//// Closes the window if the button is clicked
//void QuitButton::onClick() {
//	this->window.close();
//}
